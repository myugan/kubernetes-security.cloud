---
interface Props {
  class?: string;
  as?: 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6';
}

const { class: className = '', as: Component = 'h3' } = Astro.props;
const Tag = Component;
---

<Tag class={`font-semibold leading-none tracking-tight ds-text-primary ${className}`}>
  <slot />
</Tag>
