---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class={`rounded-lg border border-gray-200 dark:border-zinc-800 bg-white dark:bg-zinc-900/40 shadow-sm dark:shadow-black/20 ${className}`}>
  <slot />
</div>
